const fs = require('fs');
const path = require('path');

// Just a scratch script to check if we can parse or if we have another file doing so
console.log("Checking FBX loader rotation characteristics...");
